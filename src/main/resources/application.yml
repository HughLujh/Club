spring :
  profiles :
    active : dev
  datasource :
    hikari:
      auto-commit : true
    driver-class-name : org.postgresql.Driver
  jwt :
    token :
      secret-key : secret
  jpa :
    properties :
      hibernate :
        dialect : org.hibernate.dialect.PostgreSQLDialect
    show-sql : true
    generate-ddl : true
    hibernate :
      ddl-auto : update